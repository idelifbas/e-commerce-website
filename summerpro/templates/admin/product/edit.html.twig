{% extends 'adminbase.html.twig' %}

{% block title %}Ürün Düzenleme{% endblock %}

{% block content %}

    <div class="content">
        <div class="col-sm-9 col-sm-offset-3 col-lg-12 col-lg-offset-0 main">
            <div class="col-lg-12">
                <br><h4>
                    <strong class="">Ürün Detay</strong>
                    <small><a href="{{ path('admin_product_new') }}" class="btn btn-info btn-fill pull-right">Ürün ekleme</a> </small>
                </h4>
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="panel-body">
                            {{ include('admin/product/_delete_form.html.twig') }}


                            <div class="col-md-12">
                                {{ include('admin/product/_form.html.twig', {'button_label': 'Update'}) }}

                                <a href="{{ path('admin_product_index') }}" class="btn btn-dark">Cancel</a>


                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

