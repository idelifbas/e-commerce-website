{% extends 'adminbase.html.twig' %}

{% block title %}Setting{% endblock %}

{% block content %}

    <div class="content">
        <div class="col-sm-9 col-sm-offset-3 col-lg-12 col-lg-offset-0 main">
            <div class="col-lg-12">
                <br><h4>
                    <strong class="">Setting</strong>
                    <small><a href="{{ path('admin_category_new') }}" class="btn btn-info btn-fill pull-right">Kategori ekleme</a> </small>
                </h4>
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="panel-body">


                            <div class="col-md-12">

                                {{ include('admin/setting/_form.html.twig', {'button_label': 'Update'}) }}

                                <a href="{{ path('admin_setting_index') }}" class="btn btn-dark">Cancel</a>

                                {{ include('admin/setting/_delete_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
